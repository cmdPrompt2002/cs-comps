#include "thing-http.h"
#include <sys/socket.h>
#include <stdio.h>

int http_attempt(char *usr, char *pass, char* protocol);
void http_output();
int delay();

int http_attempt(char *usr, char *pass, char* protocol) {
    char buffer[250];


    return 0;
}

void 